-- ════════════════════════════════════════════════════════════
-- 0026: 명함 이력 기간 필드 분리 (앱 요청 반영)
--
-- 기존: career/education이 tag_value에 "내용 · 기간" 병합 저장
-- 변경: tag_period 컬럼 분리 (선택 — 없으면 기존 병합 문자열 호환)
-- ════════════════════════════════════════════════════════════

ALTER TABLE card_tags ADD COLUMN tag_period TEXT;
